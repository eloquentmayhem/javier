# The Player class should have its own module (or be in a module with a couple of other related things)
# then make player a member of that module instead of having it here. Also it's probably better to
# initialize to None (or better yet, the actual player) instead of 0, since 0 isn't a player.
player = 0
